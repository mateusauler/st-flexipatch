My build of st-flexipatch.

The `master` branch contains a [finalized](https://github.com/bakkeby/flexipatch-finalizer) version.  
The `unfinalized` contains a raw version with the currently selected patches applied.
